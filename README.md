# dev-setup

my dev setup for mac apple silicon

## Quick Start

open the terminal and run:

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/sixsat/dev-setup/main/setup.sh)"
```

## Repository Structure

```
.
├── Brewfile
├── dotfiles
│   ├── .config
│   │   └── karabiner
│   │       └── karabiner.json
│   ├── .gitconfig
│   ├── .p10k.zsh
│   └── .zshrc
├── ghostty
│   └── config
├── setup.sh
└── vscode
    ├── keybindings.json
    └── settings.json
```
